FROM docker.elastic.co/logstash/logstash:6.5.4

# RUN mkdir -p /opt/config/elasticsearch
# RUN mkdir -p /opt/config/logstash

# COPY config/elasticsearch/update_template.sh /opt/config/elasticsearch
# COPY config/logstash/cases_mapping.json /opt/config/logstash
# COPY config/logstash/flightpax_template.json /opt/config/logstash

# CMD ["/opt/config/elasticsearch/update_template.sh"]